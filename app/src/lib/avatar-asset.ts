import type { AnimationObject } from 'lottie-react-native';

/**
 * Drop a Lottie JSON of a 3D speaking avatar at app/assets/avatar.json
 * and switch this to: export const AVATAR_LOTTIE = require('../../assets/avatar.json');
 * Until then the vector fallback renders.
 */
export const AVATAR_LOTTIE: AnimationObject | null = null;
