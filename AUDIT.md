# Katuvit — ביקורת מקצה לקצה (2026-09-06)

סריקה מלאה של האפליקציה והשרת: באגים, אבטחה, שדרוגים. כל פריט מסומן **תוקן** / **פתוח**.

## 1. אבטחה — שרת (Modal `server/transcriber.py`)

| # | ממצא | חומרה | סטטוס |
|---|------|-------|-------|
| S1 | `media_id` מהלקוח נכנס ישר לנתיב קובץ (`/media/{id}.mov`) — path traversal | גבוהה | **תוקן** — regex `^[0-9a-f]{32}\Z` (`\Z` ולא `$`: `$` מקבל newline בסוף) |
| S2 | טקסט הכתוביות נכנס גולמי לקובץ ASS — `{\tags}` ו-`\N` מאפשרים לשנות רינדור/לשבור את הקובץ; שורות חדשות שוברות פורמט | גבוהה | **תוקן** — `clean_caption_text`: מסיר `{}`, מנטרל `\`, מקפל רווחים, חותך ל-200 תווים |
| S3 | אין מגבלת גודל/אורך להעלאה → עלות GPU/אחסון בלתי מוגבלת | גבוהה | **תוקן** — 400MB, 185 שניות (ffprobe), קובץ נמחק אם נכשל |
| S4 | `transcribe` endpoint משך URL שרירותי מהלקוח (SSRF) — לא בשימוש | בינונית | **תוקן** — הוסר. גם `download` (לא בשימוש) הוסר |
| S5 | השוואת מפתח API ב-`!=` (timing) | נמוכה | **תוקן** — `hmac.compare_digest` |
| S6 | שגיאות הרשאה חזרו כ-HTTP 200 עם `{"error"}`; ב-`transcribe` tuple `(dict, 401)` נשלח כמערך JSON | בינונית | **תוקן** — `JSONResponse` עם 401/400/404/413/422/500 |
| S7 | `font_size` לא מוגבל; `int()` על זבל → 500 | נמוכה | **תוקן** — clamp 40–160, ברירת מחדל 88 |
| S8 | `template` לא מאומת (היה `.get` עם fallback — תקין), `lines` לא מאומת (KeyError → 500) | בינונית | **תוקן** — `normalize_lines` מסנן פריטים שבורים, מוריד `end<=start`, מגביל ל-3000 |
| S9 | **מדיניות הפרטיות מבטיחה מחיקה תוך 24 שעות — לא היה שום מחיקה** | גבוהה (משפטי/אמון) | **תוקן** — `cleanup_media` cron שעתי מוחק קבצים בני 24h+ |
| S10 | ffmpeg בלי timeout → תלייה עד ה-300s של Modal; `check=True` → 500 עם stack | בינונית | **תוקן** — timeout 240s, שגיאות JSON נקיות (`decode_failed`, `burn_failed`) |
| S11 | `ts()` יצר `0:00:60.00` בקצה דקה (59.999) | נמוכה | **תוקן** — `ass_time` בסנטי-שניות |
| S12 | **מפתח ה-API אפוי בתוך האפליקציה** — ניתן לחילוץ מה-IPA; מי שמחלץ יכול לצרוב GPU על חשבונך | **גבוהה — חוסם השקה** | **פתוח** — פתרון: Firebase App Check + אימות token של משתמש בשרת + מכסות לפי uid. חובה לפני App Store |
| S13 | אין rate limiting לפי משתמש | בינונית | **פתוח** — יגיע עם S12 (מכסות/קרדיטים ב-Firestore) |
| S15 | **קריטי, נתפס רק כי הפיקר החזיר סרטון שכבר ייצאנו:** מקור מאייפון הוא HEVC 10-bit HDR (HLG). ffmpeg שמר על 10-bit → הפלט H.264 **High 10**, ש-iOS לא יודע לפענח → **הסרטון המיוצא מתנגן שחור עם סאונד** באייפון. כל ייצוא מאייפון מודרני היה שבור למשתמש אמיתי | **קריטי** | **תוקן** — זיהוי HDR ב-ffprobe, tone-mapping ל-bt709 (zscale+hable), פלט 8-bit High. אומת: פרופיל High / yuv420p / bt709, צבעים טבעיים |
| S14 | ה-cron של המחיקה רץ על image ברירת-מחדל של Modal **בלי fastapi** — המודול מייבא fastapi בראשו → ה-cron היה קורס כל שעה בשקט ולא מוחק כלום (נתפס בהרצת דמה) | גבוהה | **תוקן** — image ייעודי עם fastapi; הרצה ידנית אומתה |

בדיקות: `server/test_transcriber.py` — 8 בדיקות יחידה על ה-helpers (עוברות). הבדיקה תפסה באג אמיתי (regex `$` + newline).
אימות חי מול הפריסה: מפתח שגוי→401, traversal→400, מדיה חסרה→404, lines לא-רשימה→400, קובץ לא-מדיה→400, endpoint שהוסר→404.

## 2. באגים — אפליקציה

| # | ממצא | סטטוס |
|---|------|-------|
| A1 | **RTL timeline**: ה-ScrollView של השורות ב-`row-reverse` נפתח ב-x=0 = **סוף** התמלול, לא ההתחלה. גם לא עקב אחרי השורה הפעילה | **תוקן** — גלילה לסוף בטעינה + מעקב אוטומטי ממורכז אחרי השורה הפעילה (מדידת layout לכל pill) |
| A2 | **כרטיסי הסגנון** (5) לא נכנסו לרוחב המסך → "קלאסי" (ברירת המחדל) נחתך בקצה | **תוקן** — 5 כרטיסים בגודל מחושב לרוחב המסך, בלי גלילה |
| A3 | הגדרות נטענו מ-AsyncStorage רק כשפותחים את מסך ההגדרות → ייצוא אחרי ריסטארט השתמש בברירות מחדל (1080p) גם אם נבחר 720p | **תוקן** — `loadSettings()` ב-root layout |
| A4 | `videoMaxDuration: 180` מגביל רק **צילום** מהמצלמה, לא בחירה מהגלריה → סרטון של 20 דקות היה עולה, מתומלל (עלות), ונצרב ל-arrayBuffer של מאות MB בזיכרון JS → קריסה | **תוקן** — בדיקת `asset.duration` לפני העלאה + הודעה בעברית; השרת מאמת גם |
| A5 | Home הציג את "המשך עריכה" רק אם thumbnail הצליח → אם היצירה נכשלה, **אין דרך לחזור לעורך** | **תוקן** — hero של session לא תלוי ב-thumbnail |
| A6 | בזמן העלאה+תמלול (~40 שניות) — רק ספינר ב-FAB, בלי הסבר | **תוקן** — פס סטטוס "מעלים ומתמללים… בדרך כלל חצי דקה" |
| A7 | שמירת עריכת שורה רק דרך Enter (רמז קטן); newline מודבק היה שובר את קובץ ה-ASS | **תוקן** — כפתור "שמירה" + סניטציה (newline→רווח, ריק לא נשמר) |
| A8 | `fetch` של הצריבה בלי timeout → תלייה אינסופית בניתוק רשת | **תוקן** — AbortController 270s + הודעות בעברית לניתוק/timeout |
| A9 | שגיאות שרת הוצגו כ-"השרת החזיר שגיאה (413)" | **תוקן** — מיפוי קודי שגיאה להודעות בעברית (ארוך מדי / גדול מדי / הסרטון כבר לא בשרת…) |
| A10 | Firebase `getAuth` בלי persistence ב-RN → כל פתיחה יוצרת **משתמש אנונימי חדש** (מכסות/קרדיטים לא יעבדו) | **תוקן** — `initializeAuth` + `getReactNativePersistence(AsyncStorage)` |
| A11 | `app.json` נשאר תבנית: שם "app", scheme "app", splash כחול, בלי bundleIdentifier, **בלי הרשאות Photos ב-Info.plist** → build אמיתי היה קורס בשמירה לגלריה | **תוקן** — Katuvit / com.katuvit.app / dark / הרשאות בעברית / plugins image-picker+media-library |
| A12 | אין תוויות נגישות לכפתורי אייקון (חזרה, פלוס, טאבים, סגנונות) | **תוקן** |
| A13 | גרסה "0.1" מקודדת בהגדרות | **תוקן** — מ-`expo-constants` |
| A14 | הסרטון המיוצא נטען כולו לזיכרון JS (`arrayBuffer`) — תקין עד ~3 דקות ב-1080p; מעבר לזה סיכון | **מוגן** ע"י A4 (מגבלת 3 דקות). לטווח ארוך: הזרמה לדיסק |
| A15 | Session בזיכרון בלבד — סגירת האפליקציה מאבדת את העריכה | **פתוח** — MVP מקובל; לשקול שמירה ל-AsyncStorage |
| A16 | חלון עריכת השורה נפתח **מתחת למקלדת** — שדה הטקסט וכפתור השמירה מוסתרים (נתפס בבדיקה ידנית אחרי A7) | **תוקן** — `KeyboardAvoidingView` |

## 2b. ממצא סביבתי קריטי — הדיסק של המק היה מלא

בזמן אימות ה-E2E ההעלאה נכשלה ב-401 "unauthorized" למרות שמפתח ה-API היה זהה ו-curl עבד. לוג
אבחוני בשרת + מאזין מקומי הראו שהאפליקציה שולחת `multipart/form-data` עם **`Content-Length: 0`**.
הסיבה: **הדיסק של המק היה מלא (140MB פנויים מתוך 460GB)**. `uploadAsync` של Expo בונה את גוף
ה-multipart בקובץ זמני — הכתיבה נכשלה בשקט → גוף ריק. גם Metro הגיש bundle ישן (הקאש לא נכתב).
תיקון: ניקוי קאשים (npm, Homebrew) → 2.2GB פנויים → ההעלאה חזרה ל-200. נוסף `ensureCacheDir()`
לפני כל העלאה/כתיבה (מגן גם על מקרה שתיקיית הקאש חסרה).
**המלצה לך:** לשחרר מקום במק — DerivedData של Xcode (8.5GB, ניתן למחוק, נבנה מחדש), Library/Caches
(3.9GB), סימולטורים ישנים (6.4GB ב-CoreSimulator/Devices; `xcrun simctl delete unavailable`).
לא מחקתי מהם כלום — זה שלך להחליט.

## 3. היגיינת ריפו

| # | ממצא | סטטוס |
|---|------|-------|
| R1 | `app/LICENSE` = רישיון MIT של **Expo** (תבנית) — בריפו ציבורי זה נראה כאילו הקוד שלך MIT | **תוקן** — הוסר |
| R2 | `server/__pycache__` במעקב git | **תוקן** — הוסר + .gitignore |
| R3 | `app/scripts/reset-project.js` שריד תבנית | **תוקן** — הוסר |
| R4 | **הריפו ציבורי** (נוצר כ-Public) | **פתוח — רק אתה יכול**: Settings → Danger Zone → Change visibility → Private |
| R5 | סודות: `.env`, `config.ts` **לא** במעקב ✓; מפתח Firebase בקוד = מזהה לקוח ציבורי בתכנון ✓ | תקין |

## 3b. שדרוג מוצר באותו יום — כתוביות קריוקי

בעקבות המשוב "זה לא נותן ערך": הפלט הצרוב היה כתוביות סטטיות. עכשיו:
- **מנוע קריוקי בשרת** על בסיס חותמות הזמן של המילים מהמודל: `highlight` (המילה שנאמרת בצהוב), `reveal` (מילים נכנסות כשנאמרות), `static`.
- **4 לוקים מעוצבים**: בולט (קופסה שחורה + מילה צהובה, הלוק של Captions), גלישה, נקי, קלאסי. אותם מזהים בשרת ובאפליקציה.
- **תצוגה חיה בעורך** מילה-מילה, תואמת לפלט. כיוון השורה לפי השפה (שורה באנגלית לא מתהפכת).
- עריכת טקסט שומרת תזמונים כשמספר המילים לא השתנה; אחרת פיזור אחיד. השרת מאמת ונופל לפיזור אחיד אם הטקסט לא תואם.
- אומת מקצה לקצה: ייצוא מהאפליקציה → קובץ 1080x1920, H.264 High, yuv420p, bt709 (מתנגן באייפון), המילה הפעילה צהובה בפריים.

## 3c. מעבר ל-Google Cloud (אותו פרויקט כמו Firebase), 2026-09-06 ערב

Modal השבית את ה-workspace (חסימת חיוב). לפי בקשתו עברנו ל-**Cloud Run עם GPU L4** בפרויקט
katuvit-6bb08, חיוב "Firebase Payment". ארכיטקטורה חדשה: האפליקציה מקבלת כתובת חתומה, מעלה
ישירות ל-Cloud Storage (עם אחוזים), השרת מתמלל וצורב, ומחזיר כתובת הורדה חתומה. הסרטון לא
עובר דרך השרת. מחיקה אחרי יום = חוק של הדלי. מכונה אחת מקסימום, תקציב 400₪ עם התראות
ב-50/150/400. אומת מקצה לקצה גם ב-curl וגם באפליקציה (ייצוא 1080p, H.264 High, קריוקי).
לקח: Google מחזירה 404 משלה על הנתיב `/healthz`, לכן ה-health endpoint הוא `/health`.
Modal נעצר (ניתן לפרוס מחדש). קוד: server/cloudrun/, server/captions.py (מנוע משותף).

## 3d. חשבונות, מכסות ותשלום (2026-09-06 ערב), סוגר את S12/S13

- **זהות:** כל משתמש מתחיל כאורח (Firebase Anonymous, הופעל). Apple/Google יתקשרו לאותו uid אחר כך.
- **הרשאה לשרת:** כל בקשה חייבת token של Firebase (`Authorization: Bearer`). בלי token → 401. מפתח ה-API נשאר כנעילה שנייה.
- **מכסות בשרת** (`users/{uid}` ב-Firestore): 3 חינם לכל החיים עם סימן מים, חבילות (קרדיטים), Pro עד 60 בחודש.
  הבדיקה לפני ההעלאה (חוסך העלאה של 50MB לחינם) ולפני התמלול; הניכוי אטומי בטרנזקציה אחרי הצלחה.
- **בעלות על מדיה:** `media/{id}` עם uid; צריבה רק לבעלים (משתמש אחר → 404).
- **מחיקת חשבון** (דרישת אפל): מסמך משתמש, רשומות מדיה, משתמש ה-Auth.
- **אפליקציה:** כניסה כאורח בעלייה, `/me` בעלייה, פס "3 סרטונים חינם" בבית, paywall (חבילות 10/29₪, 30/69₪, Pro 39₪), סעיף חשבון בהגדרות עם מחיקה. 402 → paywall.
- **אומת ב-curl:** בלי token 401 → אורח חדש free_left 3 → 3 תמלולים מנכים 2,1,0 → רביעי 402 → upload-url 402 → צריבה עם סימן מים → מחיקת חשבון 200 → צריבה עם token של משתמש אחר 404.
- **עדיין פתוח:** Sign in with Apple/Google ותשלומים דורשים Apple Developer + development build; RevenueCat לאימות רכישות בשרת (`/redeem`).

## 3e. סטודיו סגנון + בית אפליקטיבי (2026-09-06 לילה)

משוב: "אפס פיצ'רים, בלי חיים". נבנה:
- **8 לוקים** במנוע: בולט, קופסה על המילה (סטייל MrBeast, טריק BorderStyle 3 עם קופסאות שקופות), קריוקי מילוי אמיתי (`\kf`), ניאון (זוהר בצבע ההדגשה), גלישה, נקי, מסגרת לבנה, קלאסי.
- **פרמטרים**: 6 צבעי הדגשה, 3 פונטים עבריים (Rubik, Heebo, Secular One, OFL, אפויים ב-image), 3 מיקומים, אנימציית קפיצה. הכל ברשימות לבנות בשרת (`style_options`).
- **עורך**: פאנל סטודיו עם לשוניות לוק/צבע/פונט/מיקום/תנועה ותצוגה חיה על הסרטון. 20 בדיקות יחידה.
- **בית**: רקע שחור טהור, פלטה ניטרלית, בלי דמות, בלי כותרת שיווקית. "הסרטונים שלך" + כרטיס "סרטון חדש" עם הדגמה חיה + רשת "סגנון הכתוביות" שקובעת ברירת מחדל.
- **חיתוך**: עורך החיתוך המובנה של iOS בבוחר (allowsEditing), מוגבל ל-3 דקות; קידוד יצוא H.264 1080p (ברירת המחדל ייצרה 295MB מקליפ של 49MB).
- לקח: Cloud Build נכשל על נתיבי פונטים סטטיים שלא קיימים ב-google/fonts; משתמשים בקבצי הפונט המשתנים.

## 3f. יכולות עריכה (2026-09-06 לילה), "הכל חוץ מתרגום"

- **בעורך**: ביטול פעולה (היסטוריה של 30 צעדים), גודל כתוביות כלשונית בסטודיו, כפתור ✨ לניקוי מילות מילוי בלחיצה אחת (כאילו/יעני/אה/אמ…), גיליון עריכה לשורה עם שבבי מילים (הקשה = הדגשה קבועה בצבע ההדגשה; שבב אימוג'י מוצע לפי מילות מפתח), פיצול/איחוד/מחיקה של שורה, הזזת תזמון ±0.2 שניות.
- **בשרת**: מילות מילוי ווקאליות (אה/אממ/um) נמחקות אוטומטית אחרי התמלול; דגל `em` לכל מילה נצבע תמיד; פונט אימוג'י (Noto Color Emoji + Symbola) ב-image.
- **תוקן בדרך**: הלוק "קופסה על המילה" צבע את OutlineColour, אבל BorderStyle 3 מצייר את הקופסה מ-BackColour (`\4c`). נתפס בפריים אמיתי (מילה שחורה בלי קופסה).
- **אומת**: פיצול ו-undo באפליקציה; שלושה לוקים לא-דיפולטיים בצריבה אמיתית (ניאון/ורוד/Secular One/למעלה, מילוי/ירוק/Rubik/למטה, קופסה/תכלת/Heebo/באמצע), פונטים ומיקומים נכונים בפריימים. 22 בדיקות יחידה.
- **פתוח**: רינדור אימוג'י צבעוני ב-libass לא בטוח (ייתכן חד-גוני דרך Symbola). ייבדק בפריים אחרי הפריסה.

## 3g. מיקום חופשי בגרירה + במה לסרטון (2026-09-06 לילה)

- **גרירה כמו באינסטגרם**: הכתובית נגררת לכל מקום בפריים (Gesture Handler + Reanimated). המיקום נשמר כאחוזים
  מהפריים והשרת צורב ב-`\pos` על קנבס 1080×1920 עם עוגן מרכזי, כך שמה שרואים זה מה שנצרב. שלושת הקבועים
  (למטה/באמצע/למעלה) נשארו כקיצורים; בלשונית "מיקום" מופיע "חופשי · גררת" אחרי גרירה.
- **במה לסרטון**: הווידאו כבר לא ממלא את המסך מאחורי הכלים. הוא יושב ב"במה" משלו מעל ציר הזמן (`contain`),
  כל הפריים גלוי, ולכן כל מיקום כתובית גלוי ולא מסתתר מאחורי הכלים.
- **תיקונים**: `GestureHandlerRootView` מעל שורש האפליקציה ביטל את שולי הבטיחות (safe area) במודל העורך;
  הועבר לתוך מסך העורך והשוליים חושבו מ-`useSafeAreaInsets`. ייצוא מעורך החיתוך עם preset H.264 1080p מוריד
  את הקובץ מ-295MB ל-57MB (15Mbps), וגם ממיר HDR ל-SDR כך שהתצוגה בעורך נראית טבעית.
- **עריכת טקסט נגישה יותר**: הקשה על הכתובית, הקשה על השורה הפעילה בציר הזמן (עם אייקון עיפרון), או לחיצה
  ארוכה על כל שורה, פותחות את גיליון העריכה.
- **אומת**: גרירה בסימולטור + ייצוא מהאפליקציה: הכתובית נצרבה בדיוק בפינה השמאלית העליונה שאליה נגררה.
  מחיקה/פיצול/undo/שבבי מילים/סטפרים של זמן עובדים. בשרת: ניקוי מילות מילוי (0 נשארו), הדגשת מילה, מיקום חופשי.
- **אימוג'י**: libass מרנדר אימוג'י כגליף חד-גוני בצבע הטקסט (Noto Color Emoji הוא פונט ביטמפ ש-libass לא
  תומך בו; Symbola מספק את הצורה). זה נראה טוב כאייקון בצבע ההדגשה, אבל זה לא אימוג'י צבעוני. פתרון מלא =
  שכבת PNG (Twemoji) בהמשך.
- **לוק "קופסה"**: אחרי שני ניסיונות שגויים (OutlineColour שקוף, BackColour) הרינדור המקומי הראה ש-libass
  מצייר קופסאות BorderStyle 3 מ-OutlineColour ורק כשהוא אטום. לכן הלוק הוא: כל מילה בקופסה שחורה, המילה
  שנאמרת בקופסה בצבע ההדגשה עם טקסט שחור (בפועל הלוק של MrBeast). ניאון צריך ליטוש (ריווח מילים בזוהר).

## 4. מה שנשאר לפני השקה (לפי סדר)
1. **S12** — App Check / אימות משתמש בשרת + מכסות. בלי זה, מפתח מחולץ = חשבון Modal פתוח לכולם.
2. **R4** — להפוך את הריפו לפרטי.
3. Firebase Anonymous Auth — להפעיל את הטוגל (עדיין כבוי).
4. Apple Developer → TestFlight; שם מותג סופי; מספר וואטסאפ בהגדרות.
5. RevenueCat / Pro paywall.
6. שדרוגים: ספריית ייצואים במסך הבית + ייצוא מחדש בלוק אחר, שמירת session, הזרמת ייצוא לדיסק, Lottie avatar (מחכה לקובץ), טלפרומטר V1.1.
