steps:
  - name: gcr.io/cloud-builders/docker
    args: ['build', '-f', 'cloudrun/Dockerfile', '-t', '${_IMAGE}', '.']
images: ['${_IMAGE}']
options:
  machineType: E2_HIGHCPU_8
  diskSizeGb: 100
timeout: 3600s
